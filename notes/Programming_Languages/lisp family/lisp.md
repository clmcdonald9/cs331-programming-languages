# background

late 1950s, an mit prof dev'd matchmatic formalism for desc computation
symbolic expression is an atom(word), nil, or a pair (s-expression.(s-expr.(s-expr())))

can be shortened to be separated by spaces
 (s-expr s-expr s-expr)


steve russel wrote an implementation in machine language, prog lang named lisp

lisp machines, aimed at running lisp

unified lisp standard produced in 1984, common lisp

# characteristics

simple syntax based on s expressions

program is a list or sequence of lists. first item of list is a function (procedure), 

dynamic, implicit, duck typing

good support for func proglang
new constructions can be defined.

exec can be repl

lp supports macros

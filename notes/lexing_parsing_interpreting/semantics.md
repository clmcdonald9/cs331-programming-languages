# definitions

syntax is correct struc of code,
semantics is the meaning of code

some things semantics might address
- runtime behavior (dynamic semantics)
- typing
- correctness of cases
- data structure organization

need to know what something means in order to interpret it.

lexing and parsing can be done without any knowledge of semantics, but interpreting needs it


